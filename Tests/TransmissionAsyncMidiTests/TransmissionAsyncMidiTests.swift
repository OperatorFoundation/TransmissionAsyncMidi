import XCTest
@testable import TransmissionAsyncMidi

final class TransmissionAsyncMidiTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TransmissionAsyncMidi().text, "Hello, World!")
    }
}
